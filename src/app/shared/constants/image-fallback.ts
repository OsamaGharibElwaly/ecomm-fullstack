/** Fallback when image fails to load. */
export const IMAGE_FALLBACK_URL = 'https://picsum.photos/seed/fallback/300/400';

/** Low-res placeholder shown while the main image loads (NgOptimizedImage). */
export const IMAGE_PLACEHOLDER_URL = 'https://picsum.photos/seed/placeholder/40/53';
